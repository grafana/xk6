name: Validate

on:
  # #region triggers
  workflow_dispatch:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]
  # #endregion triggers

permissions:
  contents: read

jobs:
  configure:
    name: Configure
    runs-on: ubuntu-latest
    outputs:
      go-current: ${{ steps.go-versions.outputs.current }}
      go-prev: ${{ steps.go-versions.outputs.prev }}
    steps:
      - name: Resolve Go versions
        id: go-versions
        uses: grafana/k6-ci/.github/actions/go-versions@4437a05286b70dbd87d6bce2bea5220694a40474 # v0.5.2

  validate:
    name: Validate
    needs: configure
    uses: ./.github/workflows/tooling-validate.yml
    with:
      # #region inputs
      go-version: ${{ needs.configure.outputs.go-current }}
      go-versions: '["${{ needs.configure.outputs.go-prev }}","${{ needs.configure.outputs.go-current }}"]'
      goreleaser-version: "2.18.2"
      platforms: '["ubuntu-latest", "windows-latest", "macos-latest"]'
      k6-versions: '["v2.0.0"]'
      bats: .github/validate.bats
      # #endregion inputs
